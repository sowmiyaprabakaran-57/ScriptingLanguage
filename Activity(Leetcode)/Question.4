# Create a window with a button
package require Tk

# Create the main window
set mainWindow [tk::frame .]

# Create a button with a specific background color
button $mainWindow.button -text "Click Me" -bg "lightblue"
pack $mainWindow.button

# Start the Tk event loop
tk::MainLoop
